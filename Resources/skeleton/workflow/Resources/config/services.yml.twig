#parameters:
#    workflow_{{ workflow_name_snake_case }}_key: value

services:
    workflow.{{ workflow_name_snake_case }}:
        class: {{ workflow_namespace }}\Service\{{ workflow_service_name }}
        arguments:
            - %database_host%
            - %database_name%
            - %database_user%
            - %database_password%
            - @logger
            - @doctrine.orm.entity_manager
            - @twig
